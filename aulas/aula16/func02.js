function soma (n1 = 0, n2 = 0) { // Definindo valores prévios para os parâmetros.
    return n1 + n2 // return sem a necessidade de definir number ou string.
}

var opa = soma(10, 2) // Chamada que pode ou não ter os dois valores.
console.log(opa) // Output.